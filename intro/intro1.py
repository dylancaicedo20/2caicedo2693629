#print('Hola Soacha') # Esta línea está comentada, no se ejecutará
edad=12 # Se define una variable llamada edad y se le asigna el valor de 12
print (not(edad >= 18 or edad <= 25)) # Se evalúa si la edad es menor a 18 o mayor a 25 y se niega el resultado con not()

cont=1 # Se define una variable llamada cont y se le asigna el valor de 1
#cont=cont+1 # Se incrementa el valor de cont en 1, pero está comentado
cont+=1 # Se incrementa el valor de cont en 1 mediante el operador de asignación aumentada
cont-=1 # Se decrementa el valor de cont en 1 mediante el operador de asignación aumentada
cont*=1 # Se multiplica el valor de cont por 1 mediante el operador de asignación aumentada
cont/=1 # Se divide el valor de cont por 1 mediante el operador de asignación aumentada
cont%=1 # Se calcula el módulo del valor de cont entre 1 mediante el operador de asignación aumentada

print(2**5) # Se calcula 2 elevado a la 5 mediante el operador de potencia
print(10//4) # Se calcula el cociente de la división entre 10 y 4 mediante el operador de división entera
